
version: "3.4"
networks:
  proxylampy:
    external: true
  net:
    driver: overlay
    attachable: true
services:
  exec:
    image: koromerzhin/nodejs:15.14.0
    volumes:
      - ${PWD}/app:/app
    command: sh -c "npm install"
    deploy:
      restart_policy:
        condition: none
      labels:
        - "traefik.enable=false"
  www:
    image: koromerzhin/nodejs:15.14.0
    volumes:
      - ${PWD}/app:/app
    networks:
      - proxylampy
      - net
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.socketio.rule=HostRegexp(`socketio.traefik.me`, `socketio.{ip:.*}.traefik.me`)"
        - "traefik.http.services.socketio.loadbalancer.server.port=80"